# Веб-фреймворк и ASGI-сервер
fastapi==0.115.6
uvicorn[standard]==0.34.0

# Валидация и конфигурация
pydantic==2.10.4
pydantic-settings==2.7.0
email-validator==2.2.0

# База данных (async): SQLAlchemy + драйверы PostgreSQL и SQLite
SQLAlchemy==2.0.36
asyncpg==0.30.0
aiosqlite==0.20.0
alembic==1.14.0

# AI-провайдер
anthropic==0.42.0

# Отправка почты (async SMTP)
aiosmtplib==3.0.2

# HTTP-клиент (нужен anthropic-у)
httpx==0.28.1

# Распределённый rate limiting (опционально, при заданном REDIS_URL)
redis==5.2.1
